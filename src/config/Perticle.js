import React from 'react';
import Particles from 'react-particles-js';
import prticleConfig from './Config';

const Perticle = () => {
    return (
            <Particles params={prticleConfig}></Particles>
    );
};

export default Perticle;